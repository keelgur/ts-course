class Circle {
  public readonly color: string;
  public readonly name: string;

  constructor(name: string, color: string) {
    this.color = color;
    this.name = name;
  }

  public calculateArea(): number {
    //TODO: calculateArea(); There's no instructions to fully implement radius/height/other properties to get area
    return -1;
  }
}

class Triangle {
  public readonly color: string;
  public readonly name: string;

  constructor(name: string, color: string) {
    this.color = color;
    this.name = name;
  }

  public calculateArea(): number {
    //TODO: calculateArea(); There's no instructions to fully implement radius/height/other properties to get area
    return -1;
  }
}

class Rectangle {
  public readonly color: string;
  public readonly name: string;

  constructor(name: string, color: string) {
    this.color = color;
    this.name = name;
  }

  print(): void {
    console.log("S=a*b");
  }

  public calculateArea(): number {
    //TODO: calculateArea(); There's no instructions to fully implement radius/height/other properties to get area
    return -1;
  }
}

class Square {
  public readonly color: string;
  public readonly name: string;

  constructor(name: string, color: string) {
    this.color = color;
    this.name = name;
  }

  print(): void {
    console.log("S=a*a");
  }

  public calculateArea(): number {
    //TODO: calculateArea(); There's no instructions to fully implement radius/height/other properties to get area
    return -1;
  }
}
